# Helpy Privacy Guard

Keeps AiderDesk local-first.

It blocks non-local-looking providers by default and blocks tool calls that look like tunnels, telemetry, or cloud routing.

Command:

```text
/helpy-privacy-status
```
